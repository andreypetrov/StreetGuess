<resources>

    <!--
        Base application theme for API 11+. This theme completely replaces
        AppBaseTheme from res/values/styles.xml on API 11+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
        <!-- API 11 theme customizations can go here. -->
    </style>

    <!-- Application theme. -->
    <!--
         <style name="AppTheme" parent="AppBaseTheme">

        All customizations that are NOT specific to a particular API-level can go here.
        <item name="android:actionBarStyle">@style/StreetGuessActionBar</item>
        <item name="android:windowActionBarOverlay">true</item> makes action bar to overlay content
        <item name="android:windowContentOverlay">@null</item> removes action bar border and shadow
    </style>

    ActionBar styles
    <style name="StreetGuessActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@null</item>
        <item name="android:titleTextStyle">@style/ActionBarTitleText</item>
    </style>

    <style name="ActionBarTitleText">
        <item name="android:textSize">@dimen/text_size_large</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/Green</item>
    </style>
    -->


</resources>